 <section></section>
        <div class="chatblock">
            <div class="box">

<form action="/edituser/{{id}}" method="POST" class="book-form">

    <h2>Edit Profile</h2>

    {{#each rows}}

    <div class="form-group">
        <label for="name">Name</label>
        <input type="text" name="name" value="{{name}}"/>
    </div>

    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" name="email" value="{{email}}"/>
    </div>

    <div class="form-group">
        <label for="favoritebook">Favorite Book</label>
        <input type="text" name="favoritebook" value="{{favoritebook}}" />
    </div>

    <div class="form-group">
        <label for="favoritegenre">Favorite Genre</label>
        <input type="text" name="favoritegenre" value="{{favoritegenre}}"></>
    </div>

    <button type="submit">Save Changes</button>

    {{/each}}

</form>

            </div>
        </div>
    </section>